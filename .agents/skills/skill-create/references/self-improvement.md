# Improve `skill-create`

Use this path only when the user explicitly asks to improve, refine, evolve, review for improvement, or update `skill-create`. Normal use of this Skill must not modify it.

Treat `skill-create` as the target Skill. Establish its current scope and references, then inspect only user-selected, observable sessions in which it created Skills. Use repository evidence only when it is necessary to interpret those sessions.

Identify a concrete improvement hypothesis about creation behavior, disclosure, knowledge classification, scope discipline, or reporting. Make the smallest supported change to this Skill's `SKILL.md` or references. Prefer removal, consolidation, simplification, or relocation when they solve the problem better than adding text.

Before finishing, verify that the result remains a repository-agnostic creation capability, keeps `SKILL.md` concise, preserves the generated-Skill self-improvement requirement, and does not persist volatile facts. If evidence suggests changing a root template invariant or `AGENTS.md`, report that as a proposal unless the user explicitly requested the policy change.

Report the observable evidence, hypothesis, changed files, and remaining uncertainty. Do not require hidden/private reasoning or introduce collection, scoring, benchmark, CLI, Agent, or orchestration machinery.
