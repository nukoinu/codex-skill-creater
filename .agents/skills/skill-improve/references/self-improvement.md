# Improve `skill-improve`

Use this path only when the user explicitly asks to improve, refine, evolve, review for improvement, or update `skill-improve`. Normal use of this Skill must not modify it.

Apply the [improvement protocol](improvement-protocol.md) with `skill-improve` as the target. Inspect only user-selected observable sessions in which it analyzed or refined a Skill, and inspect repository evidence only when needed to interpret those sessions.

Make the smallest supported edit to this Skill's `SKILL.md` or references. Improve by removing, merging, simplifying, rewriting, or relocating guidance when that is more effective than adding text. Preserve its narrow, explicit-only refinement role; do not turn it into an evaluator, Agent framework, orchestrator, benchmark, or automatic session system.

Verify that it still analyzes only user-identified logs, does not rely on hidden/private reasoning, maintains progressive disclosure, and does not silently change root template invariants. Report any required `AGENTS.md` or template-policy change as a proposal unless the user explicitly requested it.

Report the observable evidence, improvement hypothesis, changed files, and remaining uncertainty.
